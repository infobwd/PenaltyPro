/**
 * Tailwind — คอมไพล์ตอน build ไม่ใช่ตอนรันในเบราว์เซอร์
 *
 * เดิมโหลดจาก cdn.tailwindcss.com ซึ่งเป็น "Play CDN" ที่ตัวมันเองเตือนว่า
 * ห้ามใช้บน production เพราะต้องดาวน์โหลดสคริปต์ 300KB+ แล้วคอมไพล์คลาส
 * ในเครื่องผู้ใช้ทุกครั้งที่เปิดหน้า
 *
 * ปัญหาจริงที่เจอ: เปิดผ่านแอป LINE (LIFF) แล้ว CDN ตัวนี้ไม่ทำงาน
 * ทั้งหน้าจึงไม่มี CSS เลย ตัวอักษรตกบรรทัดทีละตัว ใช้งานไม่ได้จริง
 * ตอนนี้ CSS ถูกสร้างเป็นไฟล์เดียวตอน build แล้วเสิร์ฟจากโดเมนเดียวกับเว็บ
 * ไม่ต้องพึ่งเน็ตนอกและไม่ต้องคอมไพล์ในเครื่องผู้ใช้อีก
 *
 * ⚠️ content ต้องครอบทุกไฟล์ที่มีชื่อคลาส ไม่งั้นคลาสนั้นจะไม่ถูกสร้าง
 * และหน้านั้นจะเพี้ยนแบบเงียบ ๆ (Tailwind อ่านเป็น "ข้อความ" ไม่ได้เข้าใจโค้ด
 * จึงต้องเขียนชื่อคลาสเต็ม ๆ ห้ามต่อสตริงเอง เช่น `bg-${color}-500`)
 */
import animate from 'tailwindcss-animate';

export default {
  content: [
    './index.html',
    './index.tsx',
    './App.tsx',
    './components/**/*.{ts,tsx}',
    './services/**/*.{ts,tsx}',
    './types.ts',
  ],
  theme: {
    extend: {},
  },
  // คลาส animate-in / fade-in / slide-in-from-* ที่โค้ดใช้อยู่ราว 20 จุด
  // มาจากปลั๊กอินนี้ ไม่ใช่ของ Tailwind แกน ๆ — ตอนใช้ CDN มันไม่เคยทำงานเลย
  // (แอนิเมชันไม่ขึ้น แต่ไม่พังอะไร) ใส่เข้ามาให้ทำงานตามที่ตั้งใจไว้แต่แรก
  plugins: [animate],
};
